name: Deploy frontend

on: workflow_call

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - name: Build frontend
        run: echo Собирается билд фронтенд \(прогоняются юнит тесты при сборке, запускаются линтеры\), формируется и публикуется Docker образ

  deploy-stage:
    needs: build
    runs-on: ubuntu-latest
    steps:
      - name: Deploy frontend to stage/review app
        run: echo Происходит деплой фронтенда